export class NetPackage {
    name: string = '';
    speed: string = '';
    cost: string = '';
    constructor(name: string, speed: string, cost: string) { 
        this.name = name;
        this.speed = speed;
        this.cost = cost;
    }
}
